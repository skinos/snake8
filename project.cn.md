# Skinos 项目 — 目录结构与项目描述

## 术语

- **Skinos 项目**：**`project/<项目名>/`** 目录树，根目录须有 **项目描述文件**（**`prj.json`**）；本文以**组成部分**为主线说明**项目目录下有哪些以文件或目录存在的资源**，**项目描述**里如何登记见专节对照。
- **组成部分**：**项目目录下的资源**，以**文件或目录**的形式存在——例如 **项目描述文件**（**`prj.json`**）、与键对应的**子目录**（动态库、可加载组件、可执行体、对外命令、内核模块、捆绑程序等）、可选的根 **`lib/`**、约定目录 **`bin`/`etc`/`internal`/`rootfs`**、**`res` 点名的文件/目录路径**、根上按通配收入的**零散文件**等；另有**组件配置**侧：**`<键>.cfg`** 与运行期 **`项目@键`** 的数据（**`<键>.cfg`** 也是项目根上的**文件**）。**构建与封包**时，SDK 据描述里的键名与路径找到这些**路径**并装入 **`.fpk`**。

项目以**源码与资源所在目录树**为实体；**流程与包内路径**见 **`project2fpk.cn.md`**、**`fpk2rootfs.cn.md`**；合入目标系统 rootfs 由 **`fpk-install`** 完成。

## 项目范围

单一产品功能常对应多个用户态/内核产物（**`com`**、**`exe`/`cmd`**、**`lib`**、默认 **`.cfg` / `.html`** 等），收敛在**同一 Skinos 项目目录**内；**项目描述**里写标识、各组成部分的键名，以及**编排与注册**字段（专节）。交付形态为 **FPK**，运行期通过 **HE**（**`项目@组件`**，其中**项目**为描述里的 **`name`**）访问；程序侧也使用**同一套对象名字符串**。

**约束：** **`prj.json` 顶层 `name` 必须与 `project/<项目名>/` 的目录名一致。**

---

## 好处与局限

**好处：** **各组成部分**边界清晰，**描述文件**把键名与编排写清；统一 HE 调用；FPK 安装后可按登记注册组件与调度；**`obj`** 可做别名；**`wui`** 可把页面与配置对象绑定。

**局限：** 以原生组件与框架注册为主，上手有学习曲线；**`init` 阶段顺序**由系统 **`land@init`** 约定，写错阶段易导致启动顺序问题；各 **`项目@组件.方法`** 必须在对应组件中真实存在并已注册。

---

## 组成部分

下面按**项目目录里以文件或目录存在的资源**逐项列出；**最右一列**只作**对照**：该项在**项目描述**里通常登记到哪个顶层键（**`prj.json`** 里**英文段名须原样书写**）。**`init` / `uninit` / `joint` / `wui` / `obj`** **不是**「项目树里多出来的一类文件或目录」，只是**描述文件里的若干段文字**，写在 **「项目描述文件 `prj.json`」** 专节的 **「编排与注册」** 里。

**动态库、可加载组件、可执行组件、捆绑程序、对外命令、内核模块** 通常各对应**一个与键同名的子目录**；**构建与封包**时 SDK 会把这些子目录（及可选根 **`lib/`**）、约定目录、**`res` 点名的路径**和通配资源装入 **`.fpk`**。

### 总表：组成部分与描述文件登记（对照）

| 组成部分 | 含义 | 在构建与封包中的角色 | 项目树中的常规位置 | 描述文件中常见登记（参考） |
|----------|------|----------------------|-------------------|---------------------------|
| **项目描述文件** | 根目录 **`prj.json`** | **封包与安装的输入**；从中读键名、路径与编排字段 | **`project/<项目名>/prj.json`** | 即该文件；其它内容写在同一 JSON 内 |
| **动态库（按键分子目录）** | 每键一个子目录，产出 **`lib<键>.so`** 等 | 子目录进编译树→编译→**`.so` 等按约定进包内 `lib/`** | **`project/<项目名>/<键>/`** | 顶层键 **`lib`**：`"lib": { "键": "说明" }` |
| **根目录 `lib/`** | 可选的**整棵**动态库目录 | 准备阶段整棵带入编译树；封包时再按约定把其中 **`*.so*`** 收入包内 **`lib/`** | **`project/<项目名>/lib/`** | **无单独顶层键**；与上一行 **`lib` 段子目录**是两种用法，可同时存在 |
| **可加载组件** | 键同名子目录内的可加载模块；**构建产物在包根，文件名与键同名** | 子目录进编译树→按可加载组件规则编译 | **`project/<项目名>/<键>/`** | 顶层键 **`com`** |
| **组件配置** | **`项目@键`** 的持久化数据；可选 **`<键>.cfg`** | **不单独占一类「按键拷子目录」**；**`<键>.cfg`** 多在项目根，**随其它零散文件进包** | 运行期存储；默认 **`.cfg`** 多在**项目根** | **无独立顶层键**；语义随 **`com` / `exe`**（及产品约定时的 **`cmd` / `osc`**）键 |
| **可执行组件** | 独立可执行文件 | 子目录进编译树→编译→**可执行文件进包根** | **`project/<项目名>/<键>/`** | 顶层键 **`exe`** |
| **捆绑程序** | 多为大体量或第三方源码树产出的可执行体 | **流水线与可执行组件同类**，区别在体量与模板 | **`project/<项目名>/<键>/`** | 顶层键 **`osc`** |
| **对外命令** | 独立可执行，安装为全局命令 | 编译后进包内 **`bin/<键>`**；**`fpk-install`** 对已在描述里声明的项建系统命令链接 | **`project/<项目名>/<键>/`** | 顶层键 **`cmd`** |
| **内核模块** | **`.ko`** | 按内核模块方式编译→**`*.ko` 进包根** | **`project/<项目名>/<键>/`** | 顶层键 **`ko`** |
| **资源文件** | 描述里用 **`res`** **按路径点名**要进包的文件或目录；磁盘上就是**这些路径对应的实体** | 所列路径若存在，**与其它根上匹配文件一起进包** | 依 **`res`** 写法（键多为说明，**值常为相对项目根的路径**） | 顶层键 **`res`** |
| **`bin` / `etc` / `internal` / `rootfs`** | 约定子目录，整棵进包 | **封包阶段**若存在则**整棵复制到包内同名目录** | **`project/<项目名>/` 下上述目录** | **无对应顶层键** |
| **根目录通配文件** | 脚本、图、**`*.json`/`*.cfg`/`*.html`** 等 | 按 SDK 约定进 **`.fpk` 根**；**`prj.json`** 通常与其它 **`*.json`** 一并收入。**`*.md`** 是否另有文档类安装路径**依产品而定** | **项目根** | **无独立顶层键**（**`prj.json`** 仍属本行范畴） |

**与上表右列相关的顶层键名（英文须原样写）：** **`lib`、`com`、`exe`、`osc`、`cmd`、`ko`、`res`**。**`lib`～`ko`** 各段下的**子键名**须与**子目录名**一致；**`res`** 的键值形态以工具链为准。这些键主要决定**拷谁、编谁、打进包谁**。

目录与源码组织可参考 **SDK 或本仓随附的示例树**。

---

## 项目描述文件 `prj.json`

路径：**`project/<项目名>/prj.json`**。它就是**组成部分**总表里的**项目描述文件**这一行；**同一 JSON** 里既写**项目标识**，又写**各文件/目录资源在项目描述中的键名**（见总表右列），再写下面 **编排与注册** 各段。**封包与 `fpk-install` 均以该文件为输入。**

**约束：** 顶层 **`name`** 与项目目录名一致。

**顶层元数据字段示例（与下述各段同属一个 JSON 对象）：**

```json
{
  "name": "myproj",
  "version": "1.0.0",
  "intro": "一行简介",
  "desc": "较长说明",
  "author": "维护者",
  "type": "app"
}
```

- **`name`**：必须等于文件夹名；**`项目@组件`** 中的「项目」即 **`name`**。
- **`version` / `intro` / `desc`**：构建与展示常用。
- **`type`**：如 **`root` / `admin` / `user` / `app`**，表示权限或级别意图，具体策略由产品与工具决定。

### 与各组成部分对应的登记

**`lib`～`ko`、`res`** 等顶层键与**总表**右列一致，用来给**子目录、资源路径**起名、进包。**组件配置**无独立顶层键，语义绑在 **`com` / `exe`** 等键上。键顺序无强制要求。

### 编排与注册（写在描述文件里，不是项目目录下单独的文件或目录）

**`init` / `uninit` / `joint` / `wui` / `obj`** 只存在于**本文件**中，描述**装好后谁被调用、菜单长什么样、对象别名**等；**不要求**你在项目目录里为每个键再建**同名子目录**或**单独一类资源文件**。它们与**动态库目录、可加载组件目录**这类**看得见路径的资源**不是一回事，只是**项目描述里的几段文字**。

| 顶层键（须原样写） | 作用概要 |
|-------------------|----------|
| **`init` / `uninit`** | 开机、关机（或卸载）阶段要调用的 **`项目@组件.方法`** 表，写入 **`land@init`** 等 |
| **`joint`** | **事件名 → 方法** 的映射，事件触发时调用 |
| **`wui`** | Web 菜单、页面与可见性；页面与 **`项目@组件`** 等绑定 |
| **`obj`** | **`项目@…`** 别名或转发到本项目的 **`com`** 或其它项目已注册对象 |

完整字段语义与嵌套规则见 **`doc/com/land/prj.json.cn.md`**；下文各节按**组成部分类型**摘录 JSON 片段，并在需要处**对照**本文件中的写法。

---

## `lib`（动态库）


**`prj.json` 示例：**

```json
"lib": {
  "util": "本项目通用算法库"
}
```

**目录：** `project/myproj/util/`（键名 = 目录名）。

**适合做什么：** 被 **`com` / `cmd` / `exe` / `osc`** 或其它项目链接的共享代码；对外暴露头文件与 **`lib<键>.so`**（命名随工具链，一般为 `lib` + 键名 + `.so`）。

**与其它部分的差异：** 不提供 HE 上的 **`项目@util`** 式配置对象（除非另有 **`com` / `exe`** 等组件封装）；侧重**被链接**，而不是被框架按 **`@`** 当组件调度。

---

## `com`（可加载组件）


**`prj.json` 示例：**

```json
"com": {
  "sensor": "传感器逻辑",
  "gateway": "网关逻辑"
}
```

**目录：** `project/myproj/sensor/`、`gateway/`（键名 = 子目录名）。

**构建结果：** 通常**产出文件名与目录名相同**，由框架**按名加载**。

**适合做什么：** 在进程内注册**方法**（如 **`setup` / `shut` / `list`** 及业务接口），供 **`init` / `joint`** 等调度。骨架与接口约定以 **SDK 提供的可加载组件模板**为准。

**与「组件配置」的关系：** 每个 **`com` 键**在运行期可有 **`项目@该键`** 的**配置对象**（JSON），即**组件配置**（见下一节）；不要与同名的**可加载模块文件**混为一谈。

**与 `lib` 的区别：** **`com`** 由框架按 **`@`** 加载并调度；**`lib`** 仅被链接，不是 **`@` 组件**。

**与 `cmd` / `exe` 的区别：** **`com`** 在进程内以共享模块形式加载；**`cmd`、`exe`** 是独立进程。**`exe`** 是**可执行组件**，同样可以拥有 **`项目@键`** 的组件配置；**`cmd`** 是否暴露同名 **`项目@键`** 依产品与注册约定。

---

## 组件配置（与组件键绑定）


**不是**又一种「程序文件类型」，而是：**与某个组件键**（常见于 **`"com"`、`"exe"`** 段里的键）**对应的持久化 JSON**（HE：**`项目@键`**），以及可选的出厂默认 **`项目根/<键>.cfg`**。**`com`** 是进程内可加载模块；**`exe`** 是独立进程——二者代码形态不同，但只要有同名 **`项目@键`** 的配置对象，**都叫组件配置**。

**在 `prj.json` 里：** **没有**名为 **`config` 的独立段**。例如存在 **`"com": { "sensor": … }`** 或 **`"exe": { "helper": … }`**，产品上即可对应 **`项目@sensor`、`项目@helper`** 的配置数据面；**`<键>.cfg`** 仍是**松散资源**，不单独占 JSON 键名。

**运行期行为：** 人机与程序用 **HE** 读写该对象（整对象、**`:路径`**、**`=值`**、**`|{…}`** 合并、**`={…}`** 整体替换等），与下文 **「HE 语法简表」** 一致。组件代码侧用平台约定的**读配置 / 写配置**方式对接；可执行组件侧按各自约定消费同一套 **`项目@键`** 语义。

**出厂默认（资源文件）：** 若要把初始 JSON 收进 **FPK**，常在**项目根**放 **`<键>.cfg`**，例如 **`sensor.cfg`** 对应 **`com` 键 `sensor`**，**`helper.cfg`** 对应 **`exe` 键 `helper`**。可按 **`project2fpk.cn.md`** 作为**根目录松散 `*.cfg`** 收集，或写进 **`res`**；内容为 JSON，与运行期存储的合并规则以**运行环境**为准。

**最小示例：** **`com`：** 目录 **`project/myproj/sensor/`** + 可选 **`sensor.cfg`** → **`he 'myproj@sensor'`** 等。**`exe`：** 目录 **`project/myproj/helper/`** + 可选 **`helper.cfg`** → 同样可用 **`he 'myproj@helper:…'`** 读写组件配置（以实际注册为准）。

**与 `com` / `exe` 代码部分的区别：** **组件配置**是**数据面**；**`com` 目录下的模块产物**、**`exe` 目录下的可执行文件**是**代码面**，不要混成同一种磁盘形态来理解。

**与其它根目录 `*.cfg` 的区别：** 任意 **`*.cfg`** 都可当资源打包；**与某组件键同名的 `.cfg`** 是**该键的组件配置**出厂默认的约定文件名（**`com` / `exe`** 均适用）。

**与 `wui` 的关系：** **`wui`** 里 **`config` / `object`** 等指向 **`项目@某组件`** 时，引用的是**组件配置对象**，不是磁盘上的 **`com` 模块文件**或 **`exe` 二进制**本身。

---

## `cmd`（对外命令）


**`prj.json` 示例：**

```json
"cmd": {
  "mycli": "命令行工具"
}
```

**目录：** `project/myproj/mycli/`。

**适合做什么：** 给操作员或脚本直接执行的程序；安装后通常出现在全局 **`bin`** 一类路径中，**命令名常等于键名**。

**与 `exe` 的区别：** **`cmd`** 强调「像系统命令一样用」；**`exe`** 更常作为**包内工具**或按路径调用，不一定注册成全局命令名。

**与 `com` 的区别：** **`cmd`** 是独立进程；内部若需访问 **`其它项目@组件`**，一般通过**平台 API** 等方式。若产品把 **`cmd` 键**也注册成 **`项目@键`**，则同样有**组件配置**语义，与 **`exe`** 类似，依约定而定。

---

## `exe`（可执行组件）


**`prj.json` 示例：**

```json
"exe": {
  "helper": "安装/维护辅助程序"
}
```

**目录：** `project/myproj/helper/`。

**适合做什么：** **可执行组件**：随 FPK 交付的独立可执行文件；在归档内多与 **`prj.json` 同层**或位于约定目录。与 **`com`** 一样是**组件**，可有 **`项目@helper`** 的**组件配置**及可选 **`helper.cfg`**（见「组件配置」专节）；差别在于**独立进程运行**，而不是由框架在进程内**按可加载模块**调度。

**与 `cmd` 的区别：** 见上：**暴露范围与打包位置**不同（**`cmd`** 侧重全局命令与 **`bin`** 链接策略）。

**与 `osc` 的区别：** **`exe`** 多为本项目自研小工具；**`osc`** 常表示**第三方或大型**子树。

**与 `com` 的区别：** **`com`** 为进程内可加载模块；**`exe`** 为独立进程；二者均可挂**组件配置**。

---

## `osc`（捆绑程序）


**`prj.json` 示例：**

```json
"osc": {
  "ntpclient": "捆绑的 NTP 客户端源码树"
}
```

**目录：** `project/myproj/ntpclient/`（常为自带**本目录构建规则**的完整源码子树）。

**适合做什么：** 不便拆成普通 **`lib`** 的大程序或上游源码包，仍希望与本项目**同仓、同包**构建。

**与 `exe` 的区别：** **`osc`** 强调**体量/来源**（第三方、多文件源码树）；构建与安装步骤常与 **`exe`** 同属**可执行组件**一类，但目录结构更重。

---

## `ko`（内核模块）


**`prj.json` 示例：**

```json
"ko": {
  "demo": "示例内核模块"
}
```

**目录：** `project/myproj/demo/`。

**适合做什么：** 产出 **`.ko`**，供 **`insmod` / 产品脚本**加载。

**与其它部分的差异：** 运行在**内核空间**；需要**对应版本的内核头与构建环境**。多数纯应用项目可以不声明 **`ko`**。

---

## `res`（资源文件）


**`res`** 是在 **`prj.json` 里点名路径**：哪些**文件或目录**要当**资源**打进 **`.fpk`**。磁盘上没有单独一种叫「声明」的部件，**就是那些被写进 `res` 的路径所指的资源文件（或资源目录）**。

**`prj.json` 示例：**

```json
"res": {
  "defaults": "默认数据目录"
}
```

**适合做什么：** **显式列出**要收进 **FPK** 的**非编译资源**（目录、数据、额外的 **`.cfg`**、页面等），避免只靠通配时漏收或难区分；键值具体含义依工具链。

**与根目录散落文件的区别：** 根目录 **`*.cfg`（含 **`com` / `exe`** 等组件键的 **`<键>.cfg`** 默认配置）、`*.html`、`*.json`** 等常由打包规则**直接收集**，**不必**写进 **`res`**；是否还要用 **`res`** 单独点名其它路径，取决于 **`project2fpk.cn.md`** 所归纳的打包规则与当前构建入口。

---

## `obj`（对象别名与转发）


**`prj.json` 示例：**

```json
"obj": {
  "myproj@wan": "eth",
  "myproj@portal": "content@httpd"
}
```

**语义要点：**

- **值中不含 `@`**：表示本项目下 **`com`** 子目录名，运行时会注册为 **`本项目名@值`**（上例 **`myproj@eth`**），而 **键** `myproj@wan` 成为指向该实现的**对外别名**。
- **值中含 `@`**：表示**完整目标对象 id**（上例 **`content@httpd`**），键 **`myproj@portal`** 作为别名转发到已注册的 **`content@httpd`**。

**适合做什么：** 多实例命名（如多 WAN）、对外名字与目录名不一致、或把本项目菜单/配置**挂到别的项目的组件**上。

**与单独只写 `com` 的区别：** 没有 **`obj`** 时，默认对象名就是 **`项目@com键名`**；**`obj`** 用于**别名与跨项目绑定**。

---

## `init`（开机调度）


**`prj.json` 示例（某阶段多条调用）：**

```json
"init": {
  "app": {
    "myproj@sensor.setup": "",
    "myproj@gateway.setup": ""
  }
}
```

**`prj.json` 示例（某阶段单条调用，值为字符串）：**

```json
"init": {
  "app": "myproj@main.setup"
}
```

**适合做什么：** 声明**开机某阶段**要执行的 **`项目@组件.方法`**（多为 **`.setup`**），在 FPK 注册时写入 **`land@init`** 的任务表，随后由系统启动流程**按阶段**统一调用。

**与 `joint` 的区别：** **`init`** 按**固定启动阶段**触发；**`joint`** 按**运行时事件**触发。

---

## `uninit`（关机调度）


**`prj.json` 示例：**

```json
"uninit": {
  "app": {
    "myproj@sensor.shut": "",
    "myproj@gateway.shut": ""
  }
}
```

形状与 **`init`** 相同，用于关机或卸载路径；方法名多为 **`.shut`**。

**与 `init` 的区别：** 仅**调用时机与语义**不同（收尾而非拉起）。

---

## `joint`（事件联动）


**`prj.json` 示例：**

```json
"joint": {
  "network/on": {
    "myproj@gateway.on": ""
  },
  "storage/insert": {
    "myproj@sensor.refresh": ""
  }
}
```

事件名由系统或其它组件定义；收到事件时框架会调用表中方法。处理函数应**尽快返回**。

**与 `init` 的区别：** **事件驱动** vs **启动阶段驱动**。

---

## `wui`（Web 界面注册）


**`prj.json` 示例：**

```json
"wui": {
  "settings": {
    "menu": "System",
    "cn": "参数设置",
    "en": "Settings",
    "page": "settings.html",
    "config": "myproj@sensor",
    "lang": {
      "cn": "cn.json",
      "en": "en.json"
    }
  }
}
```

**适合做什么：** 声明菜单分组、多语言标题、页面文件、以及可选的 **`config` / `object` / `attr` / `mode`** 等**显示条件**（依赖某**组件配置**对象是否存在或属性取值）。

**与 `com` / `exe` 的区别：** **`wui`** 不实现业务逻辑，只负责**界面入口与可见性**；页面背后的参数读写针对 **组件配置**（**`项目@组件`**）。业务实现多在 **`com`** 等进程内模块；**`exe`** 型可执行组件亦可作为同一 **`项目@键`** 配置的消费方（依实现而定）。

---

## HE 语法简表

对 **组件配置** 所绑定的 **`项目@组件`** 对象读写使用下表（**`.方法`** 一行多用于 **`com`** 等进程内模块；可执行组件若注册方法，语法相同；其它 HE 目标在语法相同处也可类比）：

| 目的 | 写法 |
|------|------|
| 读整份配置 | `项目@组件` |
| 读路径 | `项目@组件:路径` |
| 写属性 | `项目@组件:路径=值` |
| 合并 / 替换 JSON | `项目@组件\|{…}` / `项目@组件={…}` |
| 调方法 | `项目@组件.方法`、`项目@组件.方法[a,b]` |

Shell 中建议用单引号包裹整条 HE。**程序内**调用时使用**与上表相同的对象名**（具体接口随语言与 SDK 而定）。

---

## 完整示例：新建项目并逐项加入各部分

下面用虚构项目 **`fullsample`** 演示：**先建目录与最小项目描述，再按总表中的类型逐个增加子目录，并在项目描述里写上对应段**。具体源码请用 **SDK 各类型模板**起盘，此处只强调**目录树与描述登记**；**`ko`** 需内核环境，可按产品需要省略。

### 步骤 1：项目目录与最小项目描述（`prj.json`）

- 创建 `project/fullsample/`。
- 写入仅含身份的 `prj.json`（可先只写元数据，**暂不写**各登记段）。

```json
{
  "name": "fullsample",
  "version": "1.0.0",
  "intro": "部件齐全示例",
  "desc": "演示各部分在项目描述中的登记",
  "author": "you",
  "type": "app"
}
```

在**产品使用的「要参与构建的项目列表」**中加入 **`fullsample`**（写法依 SDK/产品而定）。

### 步骤 2：加入 `lib`

在 `prj.json` 增加：

```json
"lib": {
  "util": "示例动态库"
}
```

创建目录 **`project/fullsample/util/`**，放入库源码；用 **SDK 动态库模板**起盘并改成 **`util`** 相关命名。

### 步骤 3：加入 `com`

```json
"com": {
  "main": "主业务组件"
}
```

创建 **`project/fullsample/main/`**，用 **SDK 可加载组件模板**起盘，保证**目录名**与键 **`main`** 一致，**组件标识**与键名对齐。编译后产物名与目录名一致（**`main`**）。

### 步骤 4：组件配置（出厂默认，可选）

在已声明 **`"com": { "main": … }`** 的前提下，若需要随包交付初始 JSON，在 **`project/fullsample/`** 根目录增加 **`main.cfg`**（内容 JSON），作为 **`fullsample@main`** 的**组件配置**出厂默认，随其它松散 **`.cfg`** 一并打包（见 **`project2fpk.cn.md`**）。**`exe` 键**规则相同：例如步骤 6 的 **`helper`** 若也要出厂默认，可再加 **`helper.cfg`**，对应 **`fullsample@helper`**。

### 步骤 5：加入 `cmd`

```json
"cmd": {
  "cli": "示例命令"
}
```

创建 **`project/fullsample/cli/`**，参照 **SDK 对外命令模板**。

### 步骤 6：加入 `exe`

```json
"exe": {
  "helper": "包内辅助程序"
}
```

创建 **`project/fullsample/helper/`**，参照 **SDK 可执行组件模板**。

### 步骤 7：加入 `osc`

```json
"osc": {
  "bundle": "捆绑程序目录"
}
```

创建 **`project/fullsample/bundle/`**，参照 **SDK 捆绑程序模板**。

### 步骤 8：加入 `ko`（可选）

```json
"ko": {
  "kmod": "示例内核模块"
}
```

创建 **`project/fullsample/kmod/`**，参照 **SDK 内核模块模板**；无内核环境时可删除该段及目录。

### 步骤 9：加入 `res`（点名资源文件，若工具链使用）

```json
"res": {
  "data": "说明文字"
}
```

按打包工具要求放置 **`data`** 对应文件或目录。

### 步骤 10：加入 `obj`

在已有 **`com.main`** 的前提下：

```json
"obj": {
  "fullsample@alias": "main"
}
```

对外即可使用 **`fullsample@alias`**，与 **`fullsample@main`** 指向同一实现（注册规则见上文 **`obj`** 节）。若需指向其它项目已注册对象，可将值写为 **`其它项目@组件`**。

### 步骤 11：加入 `init` / `uninit` / `joint`

```json
"init": {
  "app": {
    "fullsample@main.setup": ""
  }
},
"uninit": {
  "app": {
    "fullsample@main.shut": ""
  }
},
"joint": {
  "network/on": {
    "fullsample@main.on": ""
  }
}
```

需在 **`main`** 组件中**实现并注册** **`setup` / `shut` / `on`**（名称与上表一致）。

### 步骤 12：加入 `wui`

```json
"wui": {
  "panel": {
    "menu": "Demo",
    "cn": "示例面板",
    "en": "Panel",
    "page": "panel.html",
    "config": "fullsample@main"
  }
}
```

在项目根或约定位置放置 **`panel.html`**；语言文件可按需增加。

### 步骤 13：合并后的 `prj.json` 全貌（参考）

```json
{
  "name": "fullsample",
  "version": "1.0.0",
  "intro": "部件齐全示例",
  "desc": "演示各部分在项目描述中的登记",
  "author": "you",
  "type": "app",
  "lib": { "util": "示例动态库" },
  "com": { "main": "主业务组件" },
  "cmd": { "cli": "示例命令" },
  "exe": { "helper": "包内辅助程序" },
  "osc": { "bundle": "捆绑程序目录" },
  "ko": { "kmod": "示例内核模块" },
  "res": { "data": "资源说明" },
  "obj": { "fullsample@alias": "main" },
  "init": { "app": { "fullsample@main.setup": "" } },
  "uninit": { "app": { "fullsample@main.shut": "" } },
  "joint": { "network/on": { "fullsample@main.on": "" } },
  "wui": {
    "panel": {
      "menu": "Demo",
      "cn": "示例面板",
      "en": "Panel",
      "page": "panel.html",
      "config": "fullsample@main"
    }
  }
}
```

### 步骤 14：编译、安装与验证

按 **`project2fpk.cn.md`** 完成构建与打包；安装注册后，用 **`he 'fullsample@main'`**、**`he 'fullsample@main.setup'`** 等做冒烟测试；需要时查看 **`land@init.list`** 确认 **`app`** 阶段是否包含 **`fullsample@main.setup`**。

---

## 检查清单

1. 项目根存在 **`prj.json`**；其中 **`name`** 与目录名一致。  
2. 每个 **`lib` / `com` / `cmd` / `exe` / `osc` / `ko`** 键有同名子目录；**`com`** 构建产物名也与该目录名一致。若用 **`res`** 点名资源文件，路径与键值写法**依工具链**。  
3. **`init` / `joint`** 中的字符串必须是已存在的 **`项目@组件.方法`**。  
4. **组件配置**：需要出厂默认时，在项目根为对应 **`com` / `exe`** 等**组件键**准备 **`<键>.cfg`**（与其它松散资源一并打包）；**组件实现**与**组件配置**数据面不要混成同一种磁盘形态来理解。  
5. 模板与范例以 **SDK** 为准。

**文档结构说明：** **「组成部分」**以**项目目录下文件与目录形式的资源**为主，**描述文件中常见登记**仅作右列对照；**编排与注册**只在 **「项目描述文件 `prj.json`」** 专节写，**不与文件/目录类资源并列成节**。后文按**组成部分类型**展开，并与 **Skinos SDK 默认构建与封包约定**一致。**`fullsample`** 示例演示目录与描述的增量搭建；字段与语义以 **`doc/com/land/prj.json.cn.md`** 为准。**本文只写目录、包内布局与平台行为，不引用源码路径、头文件或构建/安装程序的实现细节。**
