# `.fpk` 解压后在系统上的路径

说明**镜像集成**时：用安装程序把 **`.fpk`** 里的内容摆到**目标 rootfs** 的哪些位置。命令行与 **Skinos SDK** 常见集成方式一致：

```text
fpk-install  <目标rootfs>  <SDK安装目录build/install>  <.fpk文件或已解压目录>
```

第二参数为有效目录时，包内 **`install/include`、`install/lib`** 会装到 **`build/install`**，供本机继续编其它软件；分析**设备上**装成什么样，以第一参数（目标 root）为主。

不同产品可把「项目载荷根」「系统库目录」「命令目录」配成不同绝对路径：常见一类是 **`/usr/share/skinos/<name>/`**、**`/usr/local/lib`**、**`/usr/local/bin`**；另一类会偏向 **`/var/lib`、`/var/bin`** 等。**下表按前一类举例**；各路径之间的**相对关系**在同类产品里应保持一致，**具体绝对路径以当前产品/SDK 约定为准**。

---

## 与封包文档的衔接

包内相对路径与项目树来源见 **`project2fpk.cn.md`**；本节只写**安装到目标机**的映射。

---

## 场景 A：经安装程序集成（出厂 rootfs / 构建集成）

下面 **`name`** 表示 **`prj.json`** 里的 **`name`**（须与 **`project/<项目名>/`** 目录名一致），举例用 **`demo`**。

| 包里（相对包根） | 安装到目标 rootfs（示例路径，常见一类产品） |
|------------------|-----------------------------------------------|
| **整包先整体复制** | **`/usr/share/skinos/<name>/`**（项目载荷目录） |
| 复制进上目录后，安装逻辑会去掉其中的 **`install/`、`etc/`、`internal/`、`rootfs/`** | 避免与下面「展开到系统其它位置」重复一份 |
| **`lib/`** 下**文件名里含 `so` 子串**的每个文件 | 在 **`/usr/local/lib/`** 建**同名符号链接**，指向 **`/usr/share/skinos/<name>/lib/该文件`** |
| **`bin/`** 下每个名字；且 **`prj.json` 的 `cmd` 段里须有该键** | 在 **`/usr/local/bin/`** 建**符号链接**，指向 **`/usr/share/skinos/<name>/bin/该文件`** |
| 包内 **`etc/`**（来自**解压后的原始包目录**，不是删完子目录之后的那份） | 合并到目标 **`/etc/`** |
| 包内 **`internal/`** | 合并到 **`/mnt/skinos/internal/`**（或产品等价路径） |
| 包内 **`rootfs/`** 下内容 | 按相对路径铺到目标 **`/`** 根下 |
| 包内 **`config/`** 下每个文件 | 以**文件名**落到 **`/usr/share/skinos/`** 根下，**不是** **`/usr/share/skinos/<name>/config/`** |

若 **`prj.json` 读不到合法 `name`**，安装程序**通常不做**「整包拷到 **`/usr/share/skinos/<name>/`**」这一段，但 **`install/`** 以及 **`etc` / `internal` / `rootfs` / `config`** 的合并仍可能发生（例如异常包只有叠加文件）。

---

## 场景 B：扩展区整树部署（不经安装程序合并 `etc`）

部分产品把解压目录**整体**放到扩展区（如 **`/mnt/skinos/internal/skinos/<name>/`**），**不**经安装程序把 **`etc`** 展开到 **`/etc/`**，故 **`etc`** 留在项目子树里。若设备上已有**出厂集成**的 **`/usr/share/skinos/<name>/`**，可与扩展区副本**并存**；卸载扩展载荷时不应假定会删出厂目录。

扩展场景下是否在 **`/var/lib`、`/var/bin`** 等处再补链接，**由产品脚本决定**，**不是安装程序默认定式**。

---

## 另见

- **`fpk.cn.md`** — FPK 是什么、谁生成。  
- **`project2fpk.cn.md`** — 包内路径怎么从项目打出来。  
- **`project.cn.md`** — **组成部分**与 **`prj.json`**（**项目描述文件**）说明。  
- **`doc/com/land/fpk.cn.md` 或 `doc/com/land/fpk.md`** — **`land@fpk`** 等平台语义。

---

## 维护说明

**安装规则或目标路径约定**变更时，须同步更新本文举例；文件名保持 **`fpk2rootfs.cn.md`**。
