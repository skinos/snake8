# 项目目录到 `.fpk` 归档的构建流程

说明在 **Skinos SDK** 中，从 **`project/<项目名>/`** 到 **`build/<项目名>/.fpk`** 并最终得到 **`.fpk`** 的**典型顺序**：准备编译树 → 编译 → 把产物与资源填入**封包目录**再压缩。**打包入口一般在项目源码根执行**，具体命令以 SDK/产品为准。

**`prj.json`** 字段语义见 **`doc/com/land/prj.json.cn.md`**；安装后系统路径见 **`fpk2rootfs.cn.md`**；**组成部分**与**项目描述**结构见 **`project.cn.md`**。

---

## 项目名、版本与封包目录

- **`prj.json`** 的 **`name`、`version`** 决定 **`build/<项目名>/`** 及 **`xxx-yyy-硬件.fpk`** 文件名里的前两段（**硬件段**依产品配置）。
- 从 **`prj.json`** 读出 **`lib`、`com`、`exe`、`osc`、`cmd`、`ko`** 各段的**键列表**，决定**哪些子目录**要复制进**构建工作目录**、**编谁**、**打进包谁**；**`res`** 再点名额外要复制的**资源文件或目录路径**。
- **封包目录**一般是 **`build/<项目名>/.fpk`**，其下预先建好 **`lib/`、`bin/`、`etc/`、`internal/`、`rootfs/`**，用于分类装文件。

---

## 第一步：准备编译树

- 在 **`build/<项目名>/`**（或当前产品等价路径）建立**构建工作目录**。
- 若项目根有 **`lib/`**，整棵复制进构建目录。
- 再按 **`prj.json`** 里 **`lib`～`ko`** 各段的键，把**同名子目录**从项目根复制进构建目录（若存在）。
- 在构建目录下建立 **`.fpk`** 及其 **`lib`、`bin`、`etc`、`internal`、`rootfs`** 子目录。

**注意：** **`prj.json` 本体**通常不在「按键复制子目录」的循环里，而在**后续封包步骤**里与根上 **`*.json`、`*.cfg`、`*.html`** 等一起写入 **`.fpk` 根**。

---

## 第二步：编译

在**构建工作目录**里按类型编译：**动态库**（含先编再 **install** 到头文件/`.so` 供后续打包）、**可加载组件**、**可执行组件**、**内核模块**、**捆绑程序**与**对外命令**等。某子目录没有自带构建文件时，由 SDK **统一模板**代编。

---

## 第三步：写入 `.fpk` 并打包

以下 **`./`** 指**项目源码根**（**`project/<项目名>/`**）；**构建目录**即 **`build/<项目名>/`** 一侧的工作树。

| 动作（行为描述） | 结果在封包目录（`.fpk`）里的位置 |
|------------------|----------------------------------|
| 项目根 **`lib/*.so*`** | **`lib/`** |
| 项目根 **`bin/*`** | **`bin/`** |
| 项目根 **`etc/*`** | **`etc/`** |
| 项目根 **`internal/*`** | **`internal/`** |
| 项目根 **`rootfs/*`** | **`rootfs/`** |
| 根上 **`*.png` `*.jpg`、`*.json` `*.cfg` `*.html`** 以及 **`res`** 段列到的**已存在的资源路径** | **包根** |
| 根上 **`*.sh` `*.ash` `*.lua`** | **包根**，并赋予可执行权限 |
| 每个 **`com` 键**在构建目录下的**与键同名无后缀产物** | **包根**，文件名 **`<键>`** |
| 每个 **`lib` 键**的 **`lib<键>.so`** | **`lib/`**，并常见 **`lib<键>.so.0`** 符号链接 |
| 每个 **`exe` / `osc` 键**的可执行文件 **`<键>`** | **包根** |
| 每个 **`cmd` 键**的可执行文件 **`<键>`** | **`bin/<键>`** |
| 每个 **`ko` 键**的 **`*.ko`** | **包根** |
| 每个 **`lib` 键**的头文件与开发用 **`lib*.so`** | **`install/include/<键>/`**、**`install/lib/`** |

最后在**封包目录**内打 **gzip tar**，得到 **`.fpk`**：**封包目录里有什么，解压就看见什么**。若产品配置了**制品输出目录**，再把生成的 **`.fpk`** 复制过去。

---

## 最小示例（解压后包根）

假设 **`prj.json`** 里 **`name` 为 `demo`**，有 **`com` 键 `sensor`、`cmd` 键 `cli`**，则包根常见至少：

```text
prj.json
sensor
bin/cli
…
```

---

## 与镜像集成时的安装

集成 rootfs 时，通常调用 **`fpk-install`** 把**刚打好的 `.fpk`** 解进**目标 rootfs** 并做链接、合并 **`etc`** 等，见 **`fpk2rootfs.cn.md`**。

---

## 维护说明

**封包阶段的拷贝与分类规则**若在 SDK/产品中调整，请同步更新本文与 **`fpk.cn.md`、`fpk2rootfs.cn.md`**；字段语义以 **`doc/com/land/prj.json.cn.md`** 为准。**本文不写具体构建脚本文件名、包含关系或内部宏名，也不指向任何源码或头文件路径。**
