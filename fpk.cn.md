# FPK 包说明

FPK 为 **Skinos 项目**的标准交付单元：扩展名 **`.fpk`**，实质为 **gzip 压缩的 tar**。解压后目录树根级与 **`prj.json` 平级**；布局与**构建过程中生成的封包目录**（常见为 **`build/<项目名>/.fpk`**）一致。

项目目录约定、**组成部分**与 **`prj.json`** 字段说明见 **`project.cn.md`**；项目目录到包内路径的逐步规则见 **`project2fpk.cn.md`**。字段语义以 **`doc/com/land/prj.json.cn.md`** 为准。安装到目标 rootfs 的路径映射见 **`fpk2rootfs.cn.md`**。

---

## 制品命名

```text
<项目名>-<版本>-<硬件段>.fpk
```

**项目名**、**版本**取自 **`prj.json`** 的 **`name`**、**`version`**；**硬件段**由当前**板级/产品配置**决定。制品一般在 **`build/<项目名>/`** 一带生成；是否再复制到 **`build/store/`** 等**制品仓目录**由**各项目的打包入口与产品约定**决定。

---

## 归档内容构成

归档在逻辑上包含：**项目描述文件 `prj.json`**、**按 `prj.json` 各段收集的编译产物**、**项目树内按规则复制的资源**。

- **`prj.json`**：必选；工具链据其解析**项目描述**；根上其它 **`*.json`** 等通常与 **`prj.json`** 一起在**封包步骤**写入**包根**。
- **`com`**：各键对应子目录产出的**无后缀名**、与目录同名的共享模块，置于**归档根**。
- **`lib`**：**`lib/lib<键>.so`** 进入 **`lib/`**；**`install/include`、`install/lib`** 供 SDK 侧链接。
- **`cmd`**：可执行文件进入 **`bin/<键>`**。
- **`exe` / `osc`**：可执行文件置于**归档根**。
- **`ko`**：**`.ko`** 置于**归档根**。
- 项目内 **`lib/`、`bin/`、`etc/`、`internal/`、`rootfs/`** 子树按规则复制到归档内同名路径。
- 项目根 **`*.cfg`、`*.html`、`*.sh`** 等由**通配规则**收集；**`res`** 段另可点名**资源文件/目录路径**一并收入。

逐步规则见 **`project2fpk.cn.md`**（**本文不展开构建脚本名或内部宏**）。

---

## 安装与集成

镜像构建时，**安装/集成步骤**通常通过 **`fpk-install`** 完成，典型形式为 **`fpk-install <目标rootfs> <build/install> <.fpk>`**：把载荷展开到**系统约定的项目目录**（常见为 **`/usr/share/skinos/<name>/`** 一类），合并 **`etc` / `internal` / `rootfs` / `config`**，并为 **`lib` / `bin`** 建立符号链接；细则见 **`fpk2rootfs.cn.md`**。

若运行期将解压树**整体**置于扩展区（如 **`/mnt/.../skinos/<name>/`**）且**不**做 **`etc` → `/etc`** 等合并，则与上述集成路径的布局语义不同，说明问题时应区分两种场景。

---

## 与 `doc/com/land/fpk.cn.md` 的关系

**`land@fpk`** 的平台语义与生命周期见 **`doc/com/land/fpk.cn.md`**（或同目录 **`fpk.md`**）。本文档只说明**本仓库根目录**所关心的 **`.fpk` 形态**与安装集成行为，**不展开构建脚本或源码级实现**。

---

## 维护说明

**`.fpk` 封包规则或安装到 rootfs 的约定**有变时，须同步更新 **`fpk.cn.md`、`project2fpk.cn.md`、`fpk2rootfs.cn.md`、`project.cn.md`**。
