***
## Management lock function
Manage equipment function locking

#### Configuration( arch@lock )
```json
{
    "restart":"lock or not",   // [ "disable", "enable" ]
    "default":"lock or not",   // [ "disable", "enable" ]
    "factory":"lock or not"    // [ "disable", "enable" ]
}
```  

